This is a test file. This contains filler information.
Apples
Bananas
1. chocolate
2. strawberry
3. pineapple
Breaking it down:
- open("notes.txt") tells Python to open the file notes.txt
- as f: tells Python to open the file notes.txt as f
- f.read() tells Python to read the contents of the file notes.txt
- in python, indents are used to make code more readable and accurate
- f read() gave us the contents of the file notes.txt as one big string (including the newlines)
- Python didn't "understand" the lines as separate things - just handed them as separate things.
    - fine for displaying a file.
- add a for loop to iterate through the lines in the file.
- a list is needed to actually store the lines.
- git mv app.py notes.txt app FileManipulation/ cleanly moves the file to the new location.
- to find files by extension, use the built-in tool pathlib - needs to be imported.
- once imported let's start using .glob() to find all files with the extension .txt.
- That little r before the quote means "raw string — treat backslashes as plain characters, no secret codes." Both work; forward slashes are often easiest.
- Path(r"some\path\to\file.txt")
- in the for loop, use .glob(**/*) to find all files in the current directory and subdirectories.
- a shorter way to do the same thing is: .rglob("*")
- to count categories, we need to use a dictionary.
- Dictionary is a collection of key-value pairs.
